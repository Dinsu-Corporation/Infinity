# Infinity Framework

An opinionated, minimalist PHP framework featuring reflection-based DI,
immutable HTTP abstractions, and tiered environment orchestration.

## Requirements
- PHP 8.2+
- ext-yaml
- ext-json

## Installation
1. Clone the repository
2. Run `composer install`
3. Configure your server to point to `/public`
